{
    "python.envFile": "${workspaceFolder}/.env",
    "terminal.integrated.env.linux": {
        "PYTHONPATH": "${env:PYTHONPATH}:${workspaceFolder}"
    },
    "python.analysis.extraPaths": [
        "${workspaceFolder}"
    ],
    "python.analysis.exclude": ["**data/**", "**mlruns/**", "**mlruns_archive/**", "**.ipynb_checkpoints/**"],
}
